[pytest]
addopts = -q --cov=starrealms --cov-report=term-missing
python_files = tests/test_*.py
markers =
    smoke: fast startup/invariant checks
    effects: effect resolution unit tests
    mechanics: gameplay rules (buy, ally, attack, scrap)